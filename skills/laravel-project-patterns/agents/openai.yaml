interface:
  display_name: 'Laravel Project Patterns'
  short_description: 'Test patterns for Laravel projects'
  default_prompt: "Use $laravel-project-patterns to retrieve test examples matching this project's inspected code and test configuration."
policy:
  allow_implicit_invocation: true
