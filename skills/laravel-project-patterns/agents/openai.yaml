interface:
  display_name: 'Laravel Project Patterns'
  short_description: 'Laravel project code and test patterns'
  default_prompt: "Use $laravel-project-patterns through its required context router, preserving its limits and reading only the references selected for this project's code and conventions."
policy:
  allow_implicit_invocation: true
