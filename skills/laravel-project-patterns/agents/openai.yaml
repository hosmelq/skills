interface:
  display_name: 'Laravel Project Patterns'
  short_description: 'Laravel project code and test patterns'
  default_prompt: "Use $laravel-project-patterns through its required search-and-select workflow, reading only applicable examples and checking them against this project's actual code, module boundaries and test configuration."
policy:
  allow_implicit_invocation: true
