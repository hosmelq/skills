interface:
  display_name: 'Laravel Project Patterns'
  short_description: 'Laravel project code and test patterns'
  default_prompt: "Use $laravel-project-patterns through its required bounded hybrid search, checking retrieved evidence against this project's actual code, module boundaries and test configuration."
policy:
  allow_implicit_invocation: true
